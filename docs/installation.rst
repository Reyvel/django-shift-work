============
Installation
============

At the command line::

    $ easy_install django-shift-work

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-shift-work
    $ pip install django-shift-work
